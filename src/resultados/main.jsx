import React from 'react';
import ReactDOM from 'react-dom/client';
import ResultadosApp from './ResultadosApp.jsx';
import '../index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ResultadosApp />
  </React.StrictMode>,
);
